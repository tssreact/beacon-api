type User {
  _id: String!
  email: String!
  name: String!
}

type Query {
  users: [User]
}

type Mutation {
  login(email: String!, password: String!): User!
  createUser(email: String!, name: String!, password: String!): User!
}
